import { MariaDB, Redis, MongoDB, Firebase } from "../src";
import * as path from "path";

(async () => {

    let mariaDB = new MariaDB();

    mariaDB.Baca(``, {
        join : {

        }
    })

})();


